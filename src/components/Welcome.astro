<section>
    <span></span>

    <div class="welcome">
        <h1>Hostal Casa Liz</h1>

        <p>Ubicado en el centro del corazón de la vieja Managua, lo que permite estar cerca de:</p>

        <p>
            Metrocentro, Plaza Inter, Mercado Oriental, Hospital Cruz Azul, Hospital Bautista,
            Hospital Militar, y asolo 10 minutos del centro de recreación Puerto Salvador Allende
        </p>
    </div>
</section>

<style lang="scss">
    @use '@sbsys/sb-scss' as sb;

    section {
        --bg-1: -100%;
        --bg-2: 50%;
        --bg-3: 200%;

        @include sb.flex(column, center, center);
        @include sb.rounded(all space xs);
        aspect-ratio: 4 / 5;
        background-size: cover;
        overflow: hidden;

        background-image: radial-gradient(
                circle at 0% 50%,
                oklch(sb.color(primary, 50) / 0.2) var(--bg-1),
                oklch(sb.color(primary, 50) / 0.8) var(--bg-2),
                oklch(sb.color(primary, 50) / 1) var(--bg-3)
            ),
            url('/hostal-casa-liz-landing-page/casa-liz.webp');

        @include sb.min-width(5xs) {
            aspect-ratio: 1;
        }

        @include sb.min-width(2xs) {
            aspect-ratio: 16 / 9;
        }

        @include sb.min-width(2xl) {
            --bg-1: 40%;
            --bg-2: 46%;
            --bg-3: 80%;

            display: grid;
            grid-template-columns: repeat(2, 1fr);
        }
    }

    .welcome {
        @include sb.flex(column, center, center);
        @include sb.font-color(base, 10);
        @include sb.gap(all space sm);
        @include sb.padding(all space md);
        text-align: center;

        h1 {
            @include sb.font(family title, size 2xl);
        }

        p {
            @include sb.font(size md);
        }

        @include sb.min-width(xs) {
            h1 {
                @include sb.font(size 4xl);
            }

            p {
                @include sb.font(size lg);
            }
        }
    }
</style>
