<section>
    <span></span>

    <div class="welcome">
        <h1>Hostal Casa Liz</h1>

        <p>
            Estamos ubicados en el corazón de Managua. Cercanos a Metrocentro, Plaza Inter, Mercado
            Oriental, Hospital Cruz Azul, Hospital Bautista & Hospital Militar
        </p>
    </div>
</section>

<style lang="scss">
    @use '@sbsys/sb-scss' as sb;

    section {
        @include sb.rounded(all space xs);
        aspect-ratio: 16 / 9;
        background-size: cover;
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        overflow: hidden;

        background-image: radial-gradient(
                circle at 0% 50%,
                oklch(sb.color(primary, 50) / 0.2) 42%,
                oklch(sb.color(primary, 50) / 0.8) 48%,
                oklch(sb.color(primary, 50) / 1) 80%
            ),
            url('./casa-liz.webp');
    }

    .welcome {
        @include sb.flex(column, center, center);
        @include sb.font-color(secondary, 10);
        @include sb.gap(all space sm);
        @include sb.padding(all space md);
        text-align: center;

        h1 {
            @include sb.font(family title, size 4xl);
        }

        p {
            @include sb.font(size lg);
            text-align: end;
        }
    }
</style>
